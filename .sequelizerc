"use strict";

const path = require("path");

module.exports = {
  config: path.resolve(path.join(process.cwd(), "sequelize"), "config.js"),
  "seeders-path": path.resolve(
    path.join(process.cwd(), "sequelize"),
    "seeders"
  ),
  "migrations-path": path.resolve(
    path.join(process.cwd(), "sequelize"),
    "migrations"
  ),
};
